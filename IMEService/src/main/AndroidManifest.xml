<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.tvremoteime">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- "检查更新"功能：从本机跳系统安装器安装下载好的APK需要这个权限（Android 8+
         上是按调用方App单独授权的"安装未知应用"开关，MainActivity里会在没有这个
         权限时引导用户去系统设置页手动开一次）。 -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <!-- Declares the input method service -->
        <service
            android:name=".IMEService"
            android:label="@string/keyboard_name"
            android:permission="android.permission.BIND_INPUT_METHOD">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
                <action android:name="com.android.tvremoteime" />
            </intent-filter>

            <meta-data
                android:name="android.view.im"
                android:resource="@xml/methods" />
        </service>
        <service android:name="com.zxt.dlna.dmr.RenderPlayerService" />
        <service android:name="org.fourthline.cling.android.AndroidUpnpServiceImpl" />

        <!-- 控制端"元素列表"功能用：识别当前屏幕上的可点击元素并代为点击，
             需要用户在系统设置-无障碍里手动开启，是独立于ADB的另一条点击路径。 -->
        <service
            android:name=".accessibility.ScreenAccessibilityService"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="player.XLVideoPlayActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:screenOrientation="user"
            android:theme="@style/PlayerTheme">
            <intent-filter>
                <action android:name="ijkplayer.xlvideo.play" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="video/*" />
                <data android:scheme="http" />
                <data android:scheme="ftp" />
                <data android:scheme="file" />
                <data android:scheme="ed2k" />
                <data android:scheme="thunder" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="video/*" />
                <data android:scheme="http" />
                <data android:scheme="ftp" />
                <data android:scheme="file" />
                <data android:scheme="ed2k" />
                <data android:scheme="thunder" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.zxt.dlna.dmp.IJKPlayer"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:launchMode="singleTop"
            android:screenOrientation="user"
            android:theme="@style/PlayerTheme">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
            </intent-filter>
        </activity>

        <receiver android:name=".IMEServiceBroadCastReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_MOUNTED" />

                <data android:scheme="file" />
            </intent-filter>
        </receiver>

        <activity android:name=".widget.ScreenShotActivity"></activity>

        <!-- "检查更新"下载下来的APK文件在自己的私有外部存储目录里，Android 7+
             不允许把裸file://路径传给别的组件（系统安装器）用，必须通过FileProvider
             换成content://Uri，否则会抛FileUriExposedException。 -->
        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.android.tvremoteime.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>